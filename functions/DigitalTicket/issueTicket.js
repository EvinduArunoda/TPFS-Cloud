const admin = require('firebase-admin');
const DBUtil = require('../util/db_util');


exports.handler = async (data, context) => {
    const firestore = admin.firestore();  // This is to access the database (Database is configured)

    // Cpmplete the function here

}